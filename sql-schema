CREATE DATABASE task;

CREATE TABLE `task`.`faculties` (
  `id` VARCHAR(11) NOT NULL,
  `dob` DATE NULL,
  `password` BINARY(60) NULL,
  PRIMARY KEY (`id`));

CREATE TABLE `task`.`student` (
  `id` VARCHAR(11) NOT NULL,
  `img` LONGBLOB NULL,
  PRIMARY KEY (`id`));

